# リファクタリング計画書

作成日: 2026-06-24
対象: `Assets/SGC2025/Scripts/` 配下の全 C# スクリプト
目的: **就活ポートフォリオとしての品質向上**（可読性・命名の統一・責務の整理）

---

## 基本方針

1. **足りない所を整え、十分な所は触らない。**
   現状の `ObjectPool` / `EventBus` / `Factory` / `Strategy` などのパターン適用は**強み**なので原則維持。
   レビュアーは「不要に複雑化していないか（過剰設計）」も見る。直しすぎ厳禁。
2. **モジュール単位で進める。** 1システムずつ変更 → Unityで再生確認 → 次へ。
3. **`[FormerlySerializedAs]` は使わない**（モジュールごとに直し、その都度Inspectorで再割り当てする）。
   → **フィールド名を変えたモジュールは、Inspectorの割り当てが外れるため再設定が必要**（各モジュールの注意に明記）。
4. 命名は既存の[コードスタイルガイド](C:/Users/YutoImata/.claude/projects/c--Unity-3D-SGC2025/memory/code_style_guide.md)に従う。

### Unityリネームの安全度（重要）
| 操作 | scene/prefab参照 | 対応 |
|---|---|---|
| **namespace変更**（ファイル名・クラス名は維持） | **壊れない**（参照は.metaのGUID＝ファイル単位） | そのままでOK |
| **クラス名＝ファイル名の変更** | 壊れない（GUIDは.metaに紐づく。ファイルをリネームすれば.metaも追従） | ファイルごとリネーム |
| **[SerializeField]フィールド名の変更** | **Inspector値が外れる** | 変更後に手動で再割り当て |
| **publicメソッド/プロパティの変更** | コード参照のみ（scene参照なし） | 参照元を追って修正 |

---

## 1. namespace 統一（最優先・低リスク）

全 namespace を `SGC2025.*` / その他 → **`Tyotyo.*`** に統一する。ゲームプレイは `Tyotyo.InGame.*` 配下へ。

### 発見した不整合（要修正）
- 🔴 **`Polychroma.Core.Log`** … `CusLog.cs` / `LoggerSettingsSO.cs` が**別プロジェクト名のまま**。要修正。
- 🔴 **`SGC2025.Effect`（単数）と `SGC2025.Effects`（複数）が混在** … `EffectController`等は単数、`ScreenFlashEffect`/`SpriteBlinkEffect`は複数。統一する。
- 🔴 **namespace `SGC2025.Camera`** … `UnityEngine.Camera` と衝突するため、コード中で毎回 `UnityEngine.Camera.main` と書く羽目になっている。名前を変える。
- 🟡 **root `SGC2025`**（サブ名前空間なし）… `Singleton.cs` / `GreeningController.cs` / `GroundDataSO.cs` が裸の `SGC2025`。適切な階層へ。
- 🟡 **`SteamManager` だけ `SGC2025.Core`** … 他のSteam関連は `SGC2025.Ranking.Steam`。寄せる。
- 🟡 **`GameManager` がフォルダ `InGame/Ground` なのに namespace `SGC2025.Manager`** … フォルダと不一致。
- 🟡 **`SGC2025.Systems`（複数）** … フォルダは `System`（単数）。揃える。

### 移行マッピング（案）
| 現 namespace | 新 namespace |
|---|---|
| `SGC2025`（Singleton） | `Tyotyo.Core` |
| `SGC2025`（GroundDataSO） | `Tyotyo.InGame.Ground` |
| `SGC2025`（GreeningController） | `Tyotyo.Effect` |
| `SGC2025.Core` | `Tyotyo.Core` |
| `SGC2025.Core.Scene` | `Tyotyo.Core.Scene` |
| `Polychroma.Core.Log` | `Tyotyo.Core.Log` |
| `SGC2025.Systems` | `Tyotyo.System` |
| `SGC2025.Audio` | `Tyotyo.Audio` |
| `SGC2025.Effect` ＋ `SGC2025.Effects` | `Tyotyo.Effect`（統一） |
| `SGC2025.Manager`（全体管理：SceneController等） | `Tyotyo.Manager` |
| `SGC2025.Manager`（InGame専用：GroundManager/WaveManager/InGameManager/GameManager） | `Tyotyo.InGame.Manager` |
| `SGC2025.Camera` | `Tyotyo.Cam`（`UnityEngine.Camera`衝突回避） |
| `SGC2025.Enemy` | `Tyotyo.InGame.Enemy` |
| `SGC2025.Player` | `Tyotyo.InGame.Player` |
| `SGC2025.Bullet` ／ `SGC2025.Bullet.Effects` | `Tyotyo.InGame.Bullet` |
| `SGC2025.Item` | `Tyotyo.InGame.Item` |
| `SGC2025.UI` | `Tyotyo.UI` |
| `SGC2025.Ranking` | `Tyotyo.Ranking` |
| `SGC2025.Ranking.Steam` | `Tyotyo.Ranking.Steam` |
| `SGC2025.Editor` | `Tyotyo.Editor` |

> ※ フォルダ名（`Scripts/SGC2025`）やルートの `SGC2025` フォルダはアセット参照(GUID)に影響しないが、変えるなら別途。namespace統一とは切り離して扱う。

---

## 2. 命名規則の統一（中リスク：フィールド名はInspector再割り当て）

### ローマ字・略語 → 英語へ
- `PlayerController`：`mutekiTime` / `nowMutekiTime` → `invincibleDuration` / `invincibleTimer`、`baseMovSpeed` → `baseMoveSpeed`
- 略語の展開（`anim`, `rb` などローカルは許容範囲、publicは要修正）

### C#命名規約違反
- 🔴 `PlayerController.anim` … **小文字のpublicプロパティ** → `Animator`（PascalCase）またはprivate化
- `public Vector2 moveInput { get; private set; }` 等のpublicプロパティもPascalCaseへ

### 一貫性
- イベント名・ハンドラ名（`OnXxxEvent`）の付け方を統一
- ブール値は `is/has/can` 接頭辞で統一（多くは既に準拠）

---

## 3. `Find` の除去（中リスク：SerializeField化で要再割り当て）

[参照注入の方針](C:/Users/YutoImata/.claude/projects/c--Unity-3D-SGC2025/memory/reference-injection-preference.md)に沿って `Find` を排除。

- 🔴 `ItemManager.cs:89-92` `GameObject.FindWithTag(GAUGE_TAG)` … **コード内に `// TODO: Findをやめて実装する` あり**。緑化ゲージ参照をSerializeField注入 or イベント経由に。
- 🟡 `EnemyMovement.cs:99` Playerの `FindWithTag`（フォールバック）… `PlayerDataProvider` 経由に一本化し、Find分岐を削除。

---

## 4. ロジック重複の集約（中リスク：構造変更）

同じ計算が複数箇所にコピーされている。共通化して「単一の真実の源泉」にする。

- 🔴 **ワールド座標 → セル変換** が3か所に重複
  - `GroundManager.SearchCellIndex`
  - `MiniMapTextureRenderer.WorldToCellIndex`
  - `MiniMapMarkerController.WorldToMiniMapPos`
  → `GroundDataSO`（または `GroundManager`）に `WorldToCell` / `CellToWorld` を集約し全箇所から呼ぶ。
- 🟡 **マップ範囲外判定** の重複
  - `BulletController.CheckBoundary`（boundary margin 1f）
  - `EnemyController.IsOutOfMapBounds`（margin 5f）
  → `GroundDataSO.IsInsideMap(pos, margin)` に集約。
- 🟡 **ビューポート → ワールド変換** の重複
  - `EffectController.ResolveWorldPosition`
  - `GreeningController.ResolveWorldPosition`
  → 共通ユーティリティ（例：`CameraUtil.ViewportToWorld`）へ。

---

## 5. 責務・結合度の整理（高リスク：設計変更。最後に慎重に）

- 🟡 **`GroundManager` が責務過多** … 地面状態の管理に加え、`EffectFactory.I`・`AudioManager.I`・`ItemManager.I` を直接呼び、エフェクト生成/SE再生/アイテム状態問い合わせまで担う。
  → 緑化の「結果」をイベント発行に寄せ、エフェクト/SEは購読側（専用Presenter）で行うと単一責務に近づく。
- 🟡 **Singleton直参照の多さ** … `GroundManager.I` `ItemManager.I` `EffectFactory.I` `AudioManager.I` `BulletFactory.I` 等が全体に散在＝グローバル結合。
  → 全廃は過剰。ポートフォリオでは「**要所だけコンストラクタ/SerializeField注入に置き換え、なぜSingletonを残したかを説明できる**」状態が理想。一気に変えない。
- 🟢 **`BulletController.CreateCircleSprite`**（ランタイムTexture生成）… ポートフォリオでは“その場しのぎ”に見える。プレハブに静的スプライトを持たせ、生成コードを削除。

---

## 6. モジュール別 作業順（リスク低 → 高）

各ステップ後に**Unity再生で動作確認**。フィールド名を変えたら**Inspector再割り当て**。

| 順 | 作業 | リスク | 主な対象 |
|---|---|---|---|
| 1 | **namespace統一**（§1）＋ Polychroma修正 | 低 | 全ファイル |
| 2 | **命名統一**（§2） | 中（Inspector再割当） | Player中心 → 各所 |
| 3 | **Find除去**（§3） | 中（Inspector再割当） | ItemManager / EnemyMovement |
| 4 | **重複集約**（§4） | 中 | Ground / MiniMap / Effect |
| 5 | **責務・結合整理**（§5） | 高 | GroundManager / Singleton利用箇所 |

### モジュール区切り（2以降はこの単位で1つずつ）
`Core` → `System` → `Audio` → `Effect` → `InGame.Player` → `InGame.Enemy` → `InGame.Bullet` → `InGame.Item` → `InGame.CameraControl` → `InGame.Ground/Manager` → `UI` → `Ranking/Steam`

---

## 7. 各モジュール共通チェックリスト

- [ ] namespace が `Tyotyo.*` に統一されている
- [ ] `Find` / `Resources.Load` を使っていない（SerializeField注入）
- [ ] フィールド/プロパティ/メソッド名がC#規約＋英語で統一
- [ ] 1ファイル＝1 public型（MonoBehaviour参照維持のため）
- [ ] ロジック重複がない（共通化済み）
- [ ] 責務が単一（クラス名から想像できる範囲に収まる）
- [ ] 変更後にUnityで再生し、該当機能が従来どおり動く
- [ ] フィールド改名箇所はInspector再割り当て済み

---

## 補足：ポートフォリオとして加点される追加項目（任意）
- **README / アーキテクチャ図**（EventBus中心の流れ、主要クラス責務）
- **「なぜこの設計にしたか」の説明**（Singleton/Strategy/Pool採用理由）
- 主要ロジック（重み付き抽選・移動戦略・緑化判定）への**ユニットテスト**
- コミット粒度の整理（このリファクタを意味単位で分割コミット）
