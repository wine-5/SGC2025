# 軽量化調査レポート（コード精査）

調査日: 2026-06-22
対象: `Assets/SGC2025/Scripts/` 配下の全 C# スクリプト
目的: メモリ・GC・CPU・描画負荷の原因箇所の洗い出し（メモリ8GB環境で動作しない問題の調査）

---

## 総評

コードの実装品質は全体的に高く、**プール化・Transformキャッシュ・イベント購読の解除対応はほぼ完璧**で、典型的な「Update内でのFind連発」「購読リーク」はほとんど見られません。
そのうえで、メモリ・描画・GCに影響する箇所を **影響度順** に以下へまとめます。

> ⚠️ 8GBで落ちる主因は、**#1 のタイルGameObject大量常駐** と、コード外の **テクスチャ/オーディオのインポート設定**（後述）の掛け算である可能性が非常に高いです。

---

## 🔴 重大（メモリ・描画の主因）

### #1 GroundManager — タイル1マス=1GameObjectで数千個常駐 + 緑化のたびにDestroy/Instantiate
- ファイル: `Assets/SGC2025/Scripts/Manager/GroundManager.cs`
- 設定: `Assets/SGC2025/Scripts/InGame/Ground/GroundDataSO.cs:14-17`（既定 `columns=60 × rows=45 = 2,700枚`）

**問題点**
- `SetStageObject()`（L265-288）で開始時に **2,700個の GameObject を Instantiate**。各タイルが SpriteRenderer（＋プレハブ次第で Collider）を持つため、**常駐メモリ・ドローコールが膨大**。
- `DrawSingleTile()`（L178-199）/ `RevertSingleTile()`（L201-223）が、緑化・復元のたびに既存タイルを `Destroy` → 新タイルを `Instantiate`。
  敵1体撃破で `size×size` マスぶん Destroy+Instantiate が走り、GCゴミとメモリ断片化を生む。
- 毎回 `$"GrassTile_{x}_{y}"`（L187, 211, 279）で文字列を生成（GCゴミ）。

**改善案（効果：最大）**
1. **タイルをGameObjectにしない。** 緑/茶の状態は既に `GroundData[,]` 配列で保持しているので、描画は
   - (A) 1枚の `Texture2D` に塗る方式（ミニマップと同方式）、または
   - (B) Unity の **Tilemap** に置き換える。
   → 2,700個のレンダラーが1〜数個になり、メモリ・ドローコールが劇的に減る。
2. それが難しければ最低限、**Destroy/Instantiate をやめて既存タイルの sprite/色を差し替えるだけ**にする（生成はStart時の1回のみ）。
3. タイルプレハブから不要な Collider を外す（弾・敵の境界判定は座標計算で行っており、タイルにColliderは不要のはず）。

---

## 🟡 中程度（GC・更新負荷）

### #2 MiniMapTextureRenderer — 1セル塗るたびに `Texture2D.Apply()`
- ファイル: `Assets/SGC2025/Scripts/UI/MiniMapTextureRenderer.cs:61-62`

`PaintCell` が毎回 `Apply()`（テクスチャ全体をGPUへ再アップロード）を呼ぶ。緑化イベントが連発するたびに発生。
**改善:** `SetPixel` のみ行い、`Apply()` は「変更があったフレームの最後に1回だけ」まとめる（ダーティフラグ方式）。

### #3 EventBus — Publishのたびにリストをコピー生成
- ファイル: `Assets/SGC2025/Scripts/Core/EventBus.cs:65`

`var handlers = new List<Delegate>(eventHandlers[eventType]);` を発行ごとに確保。
`EnemyDamageTakenEvent`（弾命中ごと）・`GroundGreenifiedEvent`（タイルごと）など高頻度イベントで毎回GCゴミが出る。
**改善:** 配列キャッシュ＋「反復中の購読変更は遅延適用」方式にしてゼロアロケーション化する。

### #4 ItemManager — Updateで毎フレーム `new List<ItemType>()`
- ファイル: `Assets/SGC2025/Scripts/InGame/Item/ItemManager.cs:279-296`（`CheckEffectExpiration`）

`Update` から毎フレーム呼ばれ、効果が無くても `new List<ItemType>()` を確保。
**改善:** リストをフィールドにキャッシュして使い回す、または逆順 `for` ループで直接 `RemoveEffect` する（リスト確保自体を無くす）。

### #5 敵スポーン経路のLINQ/文字列アロケーション
敵は数秒ごと（高Waveではより高頻度）にスポーンするため、累積でGC負荷になる。
- `Assets/SGC2025/Scripts/InGame/Enemy/Spawning/EnemySpawnConfigManager.cs:46`
  `GetValidConfigs()` がスポーンごとに `.Where(...).ToList()` で新規リストを確保（`ConfigCount` の `.Count(...)` も同様、L16）。
  → **有効な設定リストは一度だけ構築してキャッシュ**すべき。
- `Assets/SGC2025/Scripts/InGame/Enemy/Spawning/EnemyFactory.cs:52`
  `enemyData.EnemyType.ToString()` をスポーンごとに呼び、enum→string変換で文字列を確保。
  → EnemyType→プール名の対応を**辞書/事前計算でキャッシュ**する。

---

## 🟢 軽微（直すなら、の範囲）

### #6 BulletController — 円スプライトをランタイム生成し Destroy しない
- ファイル: `Assets/SGC2025/Scripts/InGame/Bullet/BulletController.cs:220-250`

`new Texture2D(64,64)` ＋ `Sprite.Create` を生成するが破棄しない。プール化されているので数は限られるが、
**シーン再読込のたびにランタイム生成 Texture2D がリーク**（Texture2DはGC対象外、明示 `Destroy` が必要）。
**改善:** プレハブに静的スプライトを設定し、ランタイム生成自体を無くすのが最善。

### #7 ボスマーカーの `OnDeath` 購読がプール再利用で蓄積
- ファイル: `Assets/SGC2025/Scripts/UI/MiniMapMarkerController.cs:152`

`enemy.OnDeath += () => RemoveBossMarker(enemy)` を登録するが、
`Assets/SGC2025/Scripts/InGame/Enemy/Core/EnemyController.cs:31-32` の `OnDeath` はプール返却時にクリアされない。
同じ敵オブジェクトがボスとして再利用されるたびにラムダが積み重なる（ボスは低頻度なので影響小）。
**改善:** `ReturnToPool()` で `OnDeath = null; OnDamageTaken = null;` を行う。

### #8 毎フレームの文字列生成（軽微なGC）
- `Assets/SGC2025/Scripts/UI/GameTimeView.cs:40` … `remainingTime.ToString("F1")` を毎フレーム。
  → 値（小数第1位）が変わった時だけ更新する。
- `Assets/SGC2025/Scripts/InGame/Enemy/Movement/MovementStrategyFactory.cs:15-26` … スポーンごとに `new XxxStrategy()`。
  状態を持つため共有はできないが、固定方向(null)以外の頻度が高い場合は留意。

### #9 GameManager.greenifiedSequence の蓄積
- ファイル: `Assets/SGC2025/Scripts/InGame/Ground/GameManager.cs:42,85-89`

緑化イベントごとに `List<Vector2Int>` へ追加（リザルトのマップ再生用）。`OnCountDownFinished` でクリアされるが、
再緑化（revert→再greenify）が多いと同一セルが重複追加され、想定以上に伸びうる。`DontDestroyOnLoad` の GameManager に乗る点も注意。
**改善:** 重複セルを追加しない、または容量に上限を設ける。

---

## ⚠️ コード外だが最優先で確認すべき（メモリの実体はここの可能性大）

「コードを全部見たい」とのことですが、8GBで落ちる主因は **アセットのインポート設定** であることが非常に多く、#1のオブジェクト数と掛け算で効きます。Unityの **Profiler → Memory** で内訳（Texture/Mesh/AudioClip/GameObject数）を見るのが最短です。

- **テクスチャ**: 非圧縮（RGBA32）/ Max Size が 2048・4096 のまま / `Read/Write Enabled` がON / UIなのにMipmap ON、になっていないか。
- **オーディオ**: 長いBGMが `Decompress On Load`（全展開でメモリ常駐）になっていないか → `Streaming` へ。
- **タイル/弾/敵プレハブ**: 余計なコンポーネント（Collider, 重いマテリアル, ParticleSystem）が付いていないか。特に #1 と組み合わさると致命的。
- **Editorで確認している場合**: Editor自体が数GB消費するため、メモリ判定はビルド版 or Profilerで行う。

---

## 着手順の推奨

1. **#1 GroundManager のタイル脱GameObject化**（Texture方式 or sprite差し替え方式）← 最大効果
2. Profiler でテクスチャ/オーディオのメモリ内訳を確認（⚠️セクション）
3. #2 ミニマップApplyの集約 → #3 EventBusのアロケーション削減 → #4 ItemManagerのリスト確保削減
4. #5 スポーン経路のキャッシュ化
5. 余力で #6〜#9

---

## 精査して問題が無かった（良好な）主な箇所

- 弾・敵・エフェクトの **ObjectPool化**（`ObjectPool.cs`, `BulletFactory.cs`, `EnemyFactory.cs`, `EffectFactory.cs`）
- EnemyMovement の **Player Transform キャッシュ**（Findは初回1回のみ／`EnemyMovement.cs:88-106`）
- EventBus購読は **全箇所でUnsubscribe対**になっておりリーク無し
- Bullet/Enemy/Player の Update はキャッシュ済みコンポーネントで動作し、毎フレームのGCはほぼ無し
- カメラ・点滅・フラッシュ系演出（`CameraManager.cs`, `SpriteBlinkEffect.cs`, `ScreenFlashEffect.cs`, `EnemyHitBlink.cs`）はイベント駆動＋アロケーションなしで良好
